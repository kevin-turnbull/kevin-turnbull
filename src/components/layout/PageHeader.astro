---
interface Props {
  eyebrow: string;
  title: string;
  lede?: string;
}

const { eyebrow, title, lede } = Astro.props;
---

<header class="chapter">
  <p class="eyebrow">{eyebrow}</p>
  <h1 class="chapter-title">{title}</h1>
  {lede && <p class="lede chapter-lede">{lede}</p>}
</header>
